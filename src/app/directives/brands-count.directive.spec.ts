import { BrandsCountDirective } from './brands-count.directive';

describe('BrandsCountDirective', () => {
  it('should create an instance', () => {
    const directive = new BrandsCountDirective();
    expect(directive).toBeTruthy();
  });
});
